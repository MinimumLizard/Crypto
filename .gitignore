# ---- Never commit: personal / confidential ----
private/
.env
.env.*
!.env.example
*.secret
secrets.yaml
config/portfolio.yaml
content/holdings*.json

# Target weights are private; only the example schema is committed (D009).
config/weights.yaml

# ---- Python ----
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
dist/
build/

# ---- Node / site ----
node_modules/
site/dist/
site/.vite/
.vite/
*.tsbuildinfo
package.json
package-lock.json

# ---- Pipeline data ----
# The history store lives on the orphan `data` branch (D008), not on main.
data/
# Built artefacts are regenerated by `terminal build` on every run.
site/public/data/

# ---- Screenshots and scratch ----
screenshots/
.playwright/
test-results/
playwright-report/
validation.txt

# ---- OS / editor ----
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
